name: Test Self-Hosted Runner

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Print hostname
        run: hostname
      - name: install git
        run: sudo apt install git -y

      - name: pull code
        run: cd /var/www/html && git pull origin main
